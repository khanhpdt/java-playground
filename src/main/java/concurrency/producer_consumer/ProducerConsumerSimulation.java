package concurrency.producer_consumer;

/**
 * Producer-consumer requirements:
 *  - The queue is bounded.
 *  - If the queue is empty, the consumer waits until there's an item.
 *  - If the queue if full, the producer waits until there's a vacancy to put the item to the queue.
 *  - The consumer removes the item from the queue when consuming an item.
 */
public class ProducerConsumerSimulation {


}
